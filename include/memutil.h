#include "string.h"

void memxor(void *, const void *, size_t);
void xormem(size_t,const void *, const void *, void *);

int memtst(void *,int,size_t);

int smemcmp(const void *c1, const void *c2, unsigned int len1, unsigned int len2);
